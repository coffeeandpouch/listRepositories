"use strict";

const searchUrl = "hhtps://www.github.com/users/{username}/repos";

function formatQueryParams(params) {}

function getRepos(username, maxResults = 10) {
  const params = {
    type: owner,
    sort: full_name,
    direction: asc,
  };
}
